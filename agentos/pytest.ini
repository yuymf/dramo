[pytest]
# Exclude old manual/integration scripts that require live env vars and
# package-installed imports (pre-existing, not part of CI unit test suite)
addopts = --ignore=tests/test_workflows.py --ignore=tests/test_actual_merge.py --ignore=tests/test_merge_images.py
