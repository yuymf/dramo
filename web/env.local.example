# Dramo Web — 本地开发环境变量
# 复制此文件为 .env.local 并按需修改

# === 后端代理 ===
# Browser-side fetch 路径前缀（保持 /api，由 Next.js API Route 代理转发）
NEXT_PUBLIC_API_URL=/api

# Server-side 代理目标（Next.js API Route → Hono 后端）
BACKEND_API_URL=http://localhost:12321
